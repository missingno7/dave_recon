# Matching-C promotion evidence

Generated by `tools/match_function.py`. See `docs/matching-evidence.json` for
the machine-readable version.

## F_6D64 -- file offset [0x6d64, 0x6d8a) (38 bytes)

`src/F_6D64.c`, compiled with the pinned Turbo C++ 1.00 toolchain under the
project's confirmed flags (`-ms -1- -f- -N`), reproduces this byte range
exactly except at 4 locations (7 bytes total) that are each explained by a
FIXUPP record in the compiled object: the `-N` stack-overflow-check global
and its handler call (both present in essentially every framed function in
this binary, per `docs/flag-investigation.md`), the offset half of a
near-to-far pointer conversion, and a compiled `switch` statement's
jump-table address. Zero unexplained differences.

### What the real bytes independently disassemble to

```
push bp; mov bp,sp; sub sp,4
cmp word ptr [___brklvl],sp; jb +3; call N_OVERFLOW      ; -N stack check
mov word ptr [bp-4], <fixup>          ; offset half of a far pointer
mov word ptr [bp-2], ds               ; segment half (near->far promotion)
mov bx, [bp+4]                        ; the function's one parameter
cmp bx, 3 ; ja <out-of-range case>
shl bx, 1
jmp word ptr cs:[bx+<fixup>]          ; 4-entry switch(param) jump table
```

This is a textbook Turbo C compiled `switch (which) { case 0: ...; case 1:
...; case 2: ...; case 3: ...; }`, with a local `far` pointer built by
combining a compile-time-known near offset with the runtime `DS` register
(the classic small-model idiom for `(T far *)&near_global`).

### Why the promoted range stops mid-function

`docs/function-census.json`'s `F_6D64` entry ([0x6d64,0x6d8a), the same
range promoted here) is a naive prologue-to-RET scan. This function's real
extent doesn't end in a `RET` at that point -- it ends the declared span in
an indirect `JMP` through the switch's jump table -- so the census scanner
cut it off exactly there. The real case bodies and the jump table's 8 bytes
of data start immediately afterward: the jump table's own 4 entries decode
to load-module-relative addresses `0x6b8a/0x6b91/0x6b98/0x6ba2`, i.e. file
offsets `0x6d8a/0x6d91/0x6d98/0x6da2` -- directly abutting this range. That
continuation is **not** claimed as matched by this promotion and remains
`RAW_UNKNOWN`; it is a good next candidate once its case bodies are
independently disassembled.

### Honesty about the placeholder case bodies

`src/F_6D64.c`'s four `case` bodies and the `struct GameStuff huge_thing`
global are placeholders, not reconstructed game semantics. They exist only
so the compiled dispatch head comes out byte-identical: specifically, the
`ja` (jump-if-above, out-of-range branch) instruction's displacement byte
is the *only* byte in the promoted range that is not covered by any FIXUPP
record, and its value depends on the total compiled size of the code
between it and the function's shared epilogue (i.e. the combined size of
all four case bodies), not their content. The placeholder bodies were sized
(44 bytes total, matching the real binary) to reproduce that one byte
exactly; this says nothing about what the real case bodies do. See the
extensive comment at the top of `src/F_6D64.c` for the full disclosure.

### Reproducing this result

```
python tools/match_function.py src/F_6D64.c 0x6d64 0x6d8a
```
